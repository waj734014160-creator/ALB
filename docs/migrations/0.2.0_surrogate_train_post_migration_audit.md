# SURROGATE_TRAIN 的 ALB 0.2 迁移后审计

## 文档角色

本文是 `0.2.0_external_consumer_audit.*` 的迁移后证据，不覆盖只读 v1 快照。
它记录 SURROGATE_TRAIN 独立分支中的消费者迁移、package 校验和剩余边界；
不保存实时训练 PID、loss、ETA，也不修改 PAPER_WORK。

## 结论

- SURROGATE_TRAIN 分支：`codex/alb-0.2-consumer-migration`。
- 审计 commit：`346256136dad592b49c3c9f410a9358268c5cb1f`。
- 23 个 declared 文件中有 20 个完成迁移提交。
- 其余 3 个是无直接 ALB import 的 context 文件；已做 smoke，按计划保持源码不变。
- 旧平铺 ALB import 数量：0。
- 原 data 与旧模型文件未覆盖；三个新 package 位于被 Git 忽略的模型目录。

## 模型 package

- `M0031`：`models/M0031_s8b_s0011_allvalid_base12_mlp_minmax01_gelu_adamw_p1000_20260609/package_v0_2`；manifest/artifact 校验=通过；Git ignored=true。
- `M0035`：`models/M0035_s8b_s0011_enormlt0p85_vnormlt0p4_edotvlt0p195_base12_mlp_minmax01_gelu_adamw_p1000_20260609/package_v0_2`；manifest/artifact 校验=通过；Git ignored=true。
- `KNN_BASELINE_20260515`：`models/train121661_valid30416_l1lt1p5_evsgeomlt0p8_scaledevs16_circular_e085_v08_s10_lrgt0p5_lroverlrlt4_mlp_minmax01_mse_noaug_h512_256_128_64_adamw_lr2em04_wd1em05_bs4096_p2500_plateau100_20260515/package_v0_2`；manifest/artifact 校验=通过；Git ignored=true。

## 验证

- ALB_MAIN 全量 pytest：325 passed、13 skipped、12 warnings、7 subtests passed（`passed`）。
- ALB surrogate package 与输入适配器：10 项通过（`passed`）。
- ALB 外部消费者、严格端口和 direct-spool 冻结参考：49 项通过（`passed`）。
- 9 个已迁移的 SURROGATE_TRAIN 命令行入口：9 个 --help smoke 均通过（`passed`）。
- M0031 package 推理：legacy/package 精确相等；package/v1 精确相等（`passed`）。
- M0035 package 推理：legacy/package 精确相等；package/v1 max_abs=2.715295792654615e-07，来自既有的跨进程 torch 数值波动（`passed_with_documented_variation`）。
- SURROGATE_TRAIN tests/test_train_albnn_contracts.py：禁用受限系统临时目录 cache provider 后 3 项通过（`passed`）。

## 本批未修复的既有问题与后续边界

- `mixed-unit-alb-data2`（migration_follow_up，`task/task_alb_data2.py`）：dimensional ALB 路径有意保留 input(nodim=True) 和 output(nodim=False)。改为严格端口前，必须先增加显式尺度适配器和独立冻结参考。
- `thermal-force-unused-spool-components`（pre_existing_behavior，`task/task_thermal_forces.py`）：采样并保存了 sx/sy，但求解时两个节流孔共用一个固定 xv，因此这两个声明输入不影响标签。
- `thermal-force-single-pad-label`（pre_existing_behavior，`task/task_thermal_forces.py`）：脚本构造四瓦 ALB 系统，但只有 thermal_pads[0] 提供力和收敛状态。
- `thermal-force-pooln-not-used`（pre_existing_behavior，`task/task_thermal_forces.py`）：文档声明的 pooln 并行模式未接入实际串行样本循环。
- `training-output-package-generation`（migration_follow_up，`run/train`）：训练仍输出松散 checkpoint 与 scaler。当前可信推理消费者已迁移，但未来 base/expert/residual run 自动生成 0.2 package 仍是独立后续任务。

完整的逐文件 pre/post SHA-256、manifest 和 artifact 哈希见同名 JSON。
